- name: SUB-A
  url: example.com/config-a.yaml
  
- name: SUB-B
  url: example.com/config-b.yaml
  # Those proxies whose names contain the following patterns will be filtered out
  patterns:
  - 最新
  - 订阅